name: pi
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.12
  - requests
  - python-dotenv
  - pandas
  - numpy
  - fastparquet
  - pyarrow == 20.0.0
  - funcy
  - httpx
  - duckdb
  - boto3
